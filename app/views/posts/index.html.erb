<% @posts.each do |post| %>
  <p>Title: <%= post.title %></p>
  <p>Body: <%= post.body %></p>
  <p>Author: 
  <% if user_signed_in? %>
    <%= User.find(post.user_id).username %></p>
  <% else %>
     Only members can see post authors 
  <% end %>
  </p>
  <br>
  <br>
<% end %>